import type { Routes } from '@angular/router';

import { IncidentListComponent } from './incident-list/incident-list';

export const fireIncidentRoutes: Routes = [{ path: '', component: IncidentListComponent }];
